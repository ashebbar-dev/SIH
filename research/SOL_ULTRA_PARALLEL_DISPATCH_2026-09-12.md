# Separate research delegation to the original Sol Ultra session

User requested this delegation on 12 September 2026, while the isolated authenticator study was closing its final review.

- Exact existing session: `01a086b4-d6d8-7b21-ae43-54d8999ed130`.
- New project write scope: `/home/user_end4/MySpace/SIH/parallel_research/sol_ultra_01a086b4_2026-09-12/`.
- Written scope and tasks: that directory's `AGENTS.md` and `TASK_BRIEF.md`.
- Preserve model/context: `gpt-5.6-sol`, reasoning effort `ultra`, existing thread (not a newly spawned substitute).
- Queue command used explicit `-C` for the lab and `-s workspace-write`. The agent is required to acknowledge effective cwd and distinguish technical sandbox narrowing from instruction-scoped isolation; OS-enforced isolation is not asserted by this controller without that check.
- First attempt could not write Codex's local runtime database under the current sandbox. After user approval, the same queue action succeeded.
- Accepted queue request: `01a09488-6a97-7c53-ab47-22ea2697aa84`.
- Acknowledgement received: effective shell cwd is the new lab, and every other SIH directory is explicitly treated as read-only. The actual workspace permission still covers the parent SIH folder: isolation is instruction-scoped, not OS-enforced.
- The acknowledgement initially used a generic GPT-6 self-description. The exact original rollout's `turn_context` at `2026-09-12T07:32:46.895Z` confirms `model=gpt-5.6-sol`, `effort=ultra`; these metadata, rather than model self-description, establish the requested setting. A non-overriding correction was queued to update the acknowledgement.
- Research is underway; completion and useful results are not implied by acknowledgement.

Research tasks: practical reproducible physical-document carriers; one genuinely matched DHRUVA speed-estimation experiment; at most one verified alternative SIH statement if the original two have no compelling near-term path. Every pilot/output must stay in the lab; existing project files are read-only. No installs, paid compute, publication, main-project integration or destructive actions.

First checkpoint requested in roughly 60–90 minutes if feasible, with at most one local compute pilot of 15 minutes for the first handoff. This timebox is a planning choice to get useful leads while the user consults GPT‑6 Pro, not an outcome-selection rule. Failed experiments must be retained.

User-facing independent ChatGPT brief: `research/GPT6_PRO_RESEARCH_PROMPT_2026-09-12.md`. It includes current measured facts, requires primary-source/code/license verification, ranks at most five serious leads and requests two decisive experiment cards with a Codex implementation brief. It does not claim the chat model has inspected the repository.

Coordination: Sol Ultra owns its lab after dispatch. The main controller owns production, presentation and current research work outside that lab; it will read returned handoffs and review any candidate before integration.

## Subsequent coordination

At 07:53 UTC, a non-replacing update was accepted as queue request `01a0949b-8fd5-7da1-a828-5961e1037d93`. It tells the original session that the main thread owns the isolated compact-code 6-pixel repetition versus 12-pixel carrier pilot, leaving the session's glyph-carrier literature and DHRUVA assignments unchanged. The queue CLI completed successfully; this is not proof of research completion.

The latest recorded session action at 07:38:49 UTC requested elevated approval to download eight public IO-VNBD CSVs into its lab. No completion output or research handoff was present at this checkpoint. The queued note asks the session to report any outstanding approval explicitly and pursue independent read-only work when possible, without bypassing approval. A stale log alone is not used as proof that a compute process is running.

A read-only live-status attempt used the documented app-server `thread/read` route, but could not connect: the sandbox denied the control socket and an approved retry reported that the default control socket did not exist. No handshake, thread read, approval decision, interrupt or resume was performed through that connection. Both proxy processes exited. Therefore a live `waitingOnApproval` status has **not** been independently confirmed; the pending-download observation remains based on the last recorded tool request.

After the user supplied GPT-6 Pro's response, additional task request `01a094ae-e6ba-7480-a8b9-6b6d1f3da5a3` was accepted at approximately 08:14 UTC. After the existing speed-state checkpoint, Sol is asked to produce a corrected D1 acquisition/provenance and prefix-feasibility sheet in its lab, using the main thread's reviewed equations and constraints. Public M/Y1/Vta candidates must be grouped and checked before any outcome inspection. At most a small development-prefix excitation diagnostic is allowed within the existing compute cap; no new model fit, monolithic EKF, active-folder edits or approval bypass. The task points to `research/GPT6_PRO_RESPONSE_DECISION_2026-09-12.md` and `research/GPT6_PRO_DHRUVA_REVIEW_2026-09-12.md` as read-only inputs. Accepted queue delivery is not a completed research result.
