# Isolated Sol Ultra research workspace

This directory belongs to the original session `01a086b4-d6d8-7b21-ae43-54d8999ed130` for the user's 12 September research delegation. It is not the main implementation workspace.

- All project writes, new code, generated datasets, reports, caches, temporary artifacts and experimental outputs must remain beneath `/home/user_end4/MySpace/SIH/parallel_research/sol_ultra_01a086b4_2026-09-12/`.
- Everything else in `/home/user_end4/MySpace/SIH` is READ-ONLY for this assignment. Do not edit, format, replace, rename, clean, delete or update production prototypes, submissions, presentations, historical evidence, plans or other agents' files.
- Do not resume previous implementation TODOs in their old locations. Read them for context and copy a minimal required source into this directory if an experiment needs modifications. Never create writable symlinks to an external project path.
- Read `TASK_BRIEF.md` first. Confirm this exact output boundary before starting; report the effective cwd and whether permissions are technically narrowed or merely instruction-scoped. Never claim OS-enforced isolation without checking it.
- When importing read-only project Python code, use `PYTHONDONTWRITEBYTECODE=1`; direct caches and temporary outputs into this directory. Existing `.venv` is read-only. No global installs, GPU/cloud jobs, downloads with unreviewed executable installers, account changes or API spending. Ask for necessary permissions rather than bypassing restrictions.
- No Git initialization, commits, pushes, publication or shared artifact changes. Use `apply_patch` for authored file edits and retain original/failed run evidence. Do not copy private key material into reports or externally shared bundles.
- Do not change thresholds, splits or baseline settings after seeing test results and present the selected run as preregistered. Separate measured outcomes, paper-reported claims and proposals.
- If subagents are used, their write scope must be disjoint subdirectories within this same lab. The root prototype and presentation remain read-only for them too.

Deliver the bounded research handoff requested in `TASK_BRIEF.md`; do not integrate anything into the main submission yourself.
