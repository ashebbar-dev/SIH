# Research scope acknowledgement

- **Only writable project scope:** `/home/user_end4/MySpace/SIH/parallel_research/sol_ultra_01a086b4_2026-09-12/`.
- **Effective shell working directory:** `/home/user_end4/MySpace/SIH/parallel_research/sol_ultra_01a086b4_2026-09-12`.
- **Isolation status:** the runtime's workspace permission is technically broader and permits writes elsewhere under `/home/user_end4/MySpace/SIH`. The folder-only boundary is therefore instruction-scoped rather than OS-enforced. I will honor it explicitly and treat every other SIH path as read-only.
- **Verified session metadata:** model `gpt-5.6-sol`; reasoning effort `ultra`.
- **External-source imports:** any Python import from read-only SIH paths will use `PYTHONDONTWRITEBYTECODE=1`; caches and generated outputs will stay inside this lab.
- **Excluded work:** no edits to active prototypes, presentations, evidence, plans, caches, or repositories; no Git operations, installations, publication, paid compute, deletion, or resumption of stale implementation tasks.

## First actions

1. Read `AGENTS.md` and `TASK_BRIEF.md` in full and accept their superseding facts and limits.
2. Inspect the cited NISHAN and DHRUVA evidence read-only before defining any experiment.
3. Investigate reproducible physical-document carriers from primary sources, then run at most one bounded local pilot with predeclared settings if a useful candidate can be isolated.
4. Define and, if feasible within the compute bound, run one matched DHRUVA speed experiment using copied inputs/code only inside this lab.
5. Record measured outcomes, paper-reported claims, proposals, negative results, and kill criteria separately in `RESEARCH_HANDOFF.md` and `EXPERIMENTS.md`.

The completed orthogonal-auth positive/transplant-negative study will not be repeated or presented as physical evidence or asymmetric security.
